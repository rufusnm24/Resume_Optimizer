# LinkedIn automation credentials
LINKEDIN_EMAIL=example@example.com
LINKEDIN_PASSWORD=changeme

# Optional: HTTP endpoint for LaTeX compilation (POST JSON {"source": "..."})
# LATEX_API_ENDPOINT=https://latex.example.com/compile
